services:
  - type: web
    name: ctrlmanage-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    region: frankfurt
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 8h
      - key: PORT
        value: 3001
      - key: FRONTEND_URL
        fromService:
          name: ctrlmanage-frontend
          type: web
          envVarKey: RENDER_EXTERNAL_URL
      - key: REDIS_URL
        fromService:
          name: ctrlmanage-redis
          type: redis
          envVarKey: REDIS_URL

  - type: redis
    name: ctrlmanage-redis
    plan: free
    region: frankfurt